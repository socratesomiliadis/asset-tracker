{
  "name": "asset-tracker",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.18.0",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "pnpm --filter @asset-tracker/shared build && concurrently -n shared,frontend,backend -c yellow,cyan,magenta \"pnpm --filter @asset-tracker/shared dev\" \"pnpm --filter frontend dev\" \"pnpm --filter backend dev\"",
    "build": "pnpm -r --if-present run build",
    "pretest": "pnpm --filter @asset-tracker/shared build",
    "test": "pnpm -r --if-present run test",
    "pretypecheck": "pnpm --filter @asset-tracker/shared build",
    "typecheck": "pnpm -r --if-present run typecheck",
    "lint": "pnpm -r --if-present run lint",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f db",
    "db:generate": "pnpm --filter backend db:generate",
    "db:migrate": "pnpm --filter backend db:migrate",
    "db:seed": "pnpm --filter @asset-tracker/shared build && pnpm --filter backend db:seed",
    "db:push": "pnpm --filter backend db:push",
    "db:check": "pnpm --filter backend db:check",
    "db:studio": "pnpm --filter backend db:studio"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
